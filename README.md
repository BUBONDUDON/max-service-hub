# 🧾 MAX Client Bot — README

Этот бот предназначен для платформы **MAX** и используется для получения оформленных заказов из мини-приложения через **Realtime** и отправки их в виде текстового сообщения пользователю.  
В сообщении также содержится кнопка **«Отказаться»**.

---

## 📦 Функциональность

- Принимает события из мини-приложения через **Supabase Realtime**
- Формирует текстовое сообщение о заказе
- Отправляет сообщение пользователю через **MAX Bot API**
- Добавляет интерактивную кнопку **«Отказаться»**
- Обрабатывает отказ через *callback-query*

---

## 🚀 Запуск проекта локально

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка окружения

Создай файл **.env** в корне проекта:

```env
TOKEN=твой_токен_бота
SUPABASE_URL=https://xxxxxxxx.supabase.co
SUPABASE_ANON_KEY=твой_anon_key
```

### 3. Сборка TypeScript

```bash
npm run build
```

### 4. Запуск бота

```bash
npm start
```

---

## 🖥 Пример запуска через командную строку

```bash
node ./dist/src/app.js
```

---

## 📁 Структура проекта

```
max-client-bot/
├── src/                                # Исходный код приложения
│   ├── app.ts                          # Главный файл приложения, точка входа
│   ├── commands/                       # Команды бота
│   │   └── command.class.ts            # Базовый класс для команд
│   ├── config/                         # Конфигурация приложения
│   │   ├── config.interface.ts         # Интерфейсы конфигурации
│   │   └── config.service.ts           # Сервис для работы с конфигурацией
│   └── supabase/                       # Работа с Supabase
│       ├── base-realtime-listener.ts   # Базовый класс для Realtime подписок
│       ├── order-listener.ts           # Слушатель событий заказов
│       └── supabase.service.ts         # Сервис для работы с Supabase
├── dist/                               # Скомпилированный JavaScript код
├── dockerfile                          # Docker конфигурация
├── package.json                        # Зависимости и скрипты проекта
├── tsconfig.json                       # Конфигурация TypeScript
└── README.md                           # Документация проекта
```

### Описание основных директорий

- **`src/`** — исходный код на TypeScript
  - **`app.ts`** — инициализация бота, настройка команд и обработчиков
  - **`commands/`** — модули команд бота
  - **`config/`** — управление конфигурацией и переменными окружения
  - **`supabase/`** — интеграция с Supabase Realtime для получения событий заказов

- **`dist/`** — скомпилированный JavaScript код (генерируется при сборке)

---

## 🐳 Запуск через Docker

### 1. Сборка Docker образа

```bash
docker build -t max-client-bot .
```

### 2. Запуск контейнера

Перед запуском убедитесь, что у вас есть файл `.env` с необходимыми переменными окружения.

#### Вариант 1: Запуск с монтированием .env файла

```bash
docker run -d --name max-bot --env-file .env max-client-bot
```

#### Вариант 2: Запуск с передачей переменных окружения

```bash
docker run -d --name max-bot \
  -e TOKEN=твой_токен_бота \
  -e SUPABASE_URL=https://xxxxxxxx.supabase.co \
  -e SUPABASE_ANON_KEY=твой_anon_key \
  max-client-bot
```

### 3. Просмотр логов

```bash
docker logs -f max-bot
```

### 4. Остановка контейнера

```bash
docker stop max-bot
```

### 5. Удаление контейнера

```bash
docker rm max-bot
```

### Особенности Docker образа

- Использует **Node.js 18** в качестве базового образа
- Автоматически устанавливает зависимости при сборке
- Выполняет сборку TypeScript проекта
- Включает **HEALTHCHECK** для мониторинга состояния контейнера
- Запускает бота через `npm start`

---

## 📚 Используемые технологии

- TypeScript
- @maxhub/max-bot-api
- Supabase Realtime
- dotenv
